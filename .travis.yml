language: node_js
node_js:
- '8'
before_install:
- curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.10.1
- export PATH=$HOME/.yarn/bin:$PATH
before_script:
- yarn
- yarn bootstrap
cache:
  yarn: true
notifications:
  email: false
